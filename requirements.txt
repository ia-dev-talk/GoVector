# GoVector backend dependencies

fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy[asyncio]==2.0.23
asyncpg==0.29.0
pydantic==2.5.2
pydantic-settings==2.1.0
python-multipart==0.0.6
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2
python-dotenv==1.0.0
alembic==1.15.2
numpy>=1.26.0
scipy>=1.11.0
pandas
openpyxl
defusedxml==0.7.1
# PDF reports: keep the renderer explicit so a .pdf response can never silently
# degrade to HTML on a fresh pilot server. 70.0 is the current security release.
weasyprint==70.0
# Starlette 0.27 TestClient still passes the removed ``app=`` argument.
# httpx 0.28+ therefore breaks the backend test suite during client creation.
httpx>=0.25.2,<0.28
